<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycrm.crm.mapper.OperatorMapper">
    <select id="selectPage" resultType="com.mycrm.crm.entity.OperatorVo">
    SELECT o.*,r.roname from operator o join roles r on o.roid=r.roid ${ew.customSqlSegment}
    </select>
    <select id="selectById" resultType="com.mycrm.crm.entity.OperatorVo">
    SELECT o.*,r.roname from operator o join roles r on o.roid=r.roid ${ew.customSqlSegment}
    </select>
    <select id="queryList" resultType="com.mycrm.crm.entity.OperatorVo">
    SELECT o.*,r.roname from operator o join roles r on o.roid=r.roid ${ew.customSqlSegment}
    </select>

</mapper>
